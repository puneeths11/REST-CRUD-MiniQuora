<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Post Card</title>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <h1>Mini Post Card</h1>
    <!----------------- loop to retrive all data of post-------------------->
    <% for(post of posts){ %>
    <div class="post">
      <h3 class="user">@<%= post.username %></h3>
      <h4 class="content"><%= post.content %></h4>
      <br />
      <br />
      <a href="http://localhost:8080/posts/<%=post.id%>">See in detail</a>
      <a href="http://localhost:8080/posts/<%=post.id%>/edit">Edit</a>
      <!-- only take id not slash symbol so need to specify slash-->
      <!-- using ejs template inside string  -->
      <form method="post" action="/posts/<%=post.id%>?_method=DELETE">
        <button>Delete Postt</button>
      </form>
    </div>
    <% } %>
    <!---------------------loop End----------------------------------->
    <br />
    <a href="http://localhost:8080/posts/new">Create New Post</a>
  </body>
</html>
